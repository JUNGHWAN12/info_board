# GitHub 및 Streamlit Community Cloud 배포 가이드

## 배포 전 확인

- GitHub에 올려도 되는 파일만 포함한다: `app.py`, `src/`, `sql/`, `requirements.txt`, `.streamlit/config.toml`, `README.md`
- 절대 올리지 않는 파일: `.streamlit/secrets.toml`, `.env`, Supabase secret key, Google OAuth Client Secret
- `.gitignore`에 `.streamlit/secrets.toml`이 포함되어 있는지 확인한다.
- Supabase SQL Editor에서 [`sql/schema.sql`](sql/schema.sql)을 실행한다.

## 1. GitHub 저장소 만들기와 업로드

1. GitHub에 로그인한 뒤 오른쪽 위 **+ → New repository**를 선택한다.
2. 저장소 이름을 정한다. 예: `info-class-board`
3. 수업에서 학생 모두가 소스까지 볼 수 있어도 되면 **Public**, 소스와 배포 앱 접근을 제한하려면 **Private**을 선택한다.
4. 이 폴더에는 이미 파일이 있으므로 GitHub의 README 초기화 옵션은 선택하지 않고 **Create repository**를 누른다.
5. PowerShell을 `F:\info_board`에서 열고 아래 명령을 실행한다. `<GITHUB_ID>`는 본인 GitHub 아이디로 바꾼다.

```powershell
git init
git add .
git status
git commit -m "Initial Streamlit class board"
git branch -M main
git remote add origin https://github.com/<GITHUB_ID>/info-class-board.git
git push -u origin main
```

`git status` 출력에 `.streamlit/secrets.toml`이나 `.env`가 보이면 `git add`나 `git commit`을 진행하지 말고 해당 파일을 제거한 뒤 다시 확인한다.

## 2. Google 로그인 설정

먼저 배포 앱의 주소를 정한다. 예: `https://info-class-board.streamlit.app`

Google Cloud Console에서 다음을 설정한다.

1. 프로젝트를 만들고 **Google Auth Platform**에서 OAuth 동의 화면을 설정한다.
2. **Credentials → Create Credentials → OAuth client ID**에서 웹 애플리케이션을 만든다.
3. 승인된 JavaScript 원본에 `https://info-class-board.streamlit.app`을 추가한다.
4. 승인된 리디렉션 URI에 `https://info-class-board.streamlit.app/oauth2callback`을 추가한다.
5. 발급된 Client ID와 Client Secret을 복사해 둔다. Secret은 GitHub에 올리지 않는다.

로컬에서도 로그인할 경우 `http://localhost:8501`과 `http://localhost:8501/oauth2callback`을 추가한다.

## 3. Streamlit Community Cloud 배포

1. [Streamlit Community Cloud](https://share.streamlit.io/)에 GitHub 계정으로 로그인하고 GitHub 연결을 승인한다.
2. 저장소 소유자에 맞는 Workspace를 선택한 뒤 **Create app**을 누른다.
3. **Yup, I have an app**을 선택한다.
4. Repository: `<GITHUB_ID>/info-class-board`, Branch: `main`, Main file path: `app.py`를 입력한다.
5. **Custom subdomain**에 위에서 정한 이름(예: `info-class-board`)을 입력한다.
6. **Advanced settings**에서 Python 3.12를 선택하고, Secrets 칸에 아래 값을 붙여 넣는다.

```toml
[auth]
redirect_uri = "https://info-class-board.streamlit.app/oauth2callback"
cookie_secret = "32자_이상의_무작위_문자열로_교체"

[auth.google]
client_id = "Google_Cloud_Client_ID"
client_secret = "Google_Cloud_Client_Secret"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

SUPABASE_URL = "https://<project-ref>.supabase.co"
SUPABASE_SERVICE_KEY = "sb_secret_..."
```

7. **Deploy**를 누르고 로그에서 패키지 설치와 앱 시작이 완료되는지 확인한다.
8. 앱 주소를 열어 Google 로그인, 학생 작품 제출, 교사 과제 생성을 각각 점검한다.

## 4. 교사 계정 활성화

교사가 Google 로그인을 한 번 마친 후 Supabase SQL Editor에서 실행한다.

```sql
update public.profiles
set role = 'teacher'
where email = '교사_Google_이메일';
```

교사가 앱을 새로고침하거나 다시 로그인하면 **과제 관리** 탭이 보인다.

## 5. 운영 및 수정

- `main` 브랜치에 push하면 Community Cloud가 변경을 감지해 자동 반영한다.
- `requirements.txt` 변경은 의존성을 다시 설치하므로 반영에 조금 더 시간이 걸린다.
- Secrets 변경은 Community Cloud 앱의 **Settings → Secrets**에서 수정 후 저장한다.
- 배포 로그는 앱의 **Manage app** 또는 Workspace에서 확인한다.
- 앱을 공개할 경우 Google OAuth 동의 화면의 테스트 사용자 제한을 해제하고, 학교 개인정보 처리 기준을 확인한다.
