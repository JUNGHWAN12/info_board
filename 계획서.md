# 정보 수업용 작품 게시판 개발 계획서

## 1. 목표

학생이 정보 수업에서 만든 웹 앱의 링크와 소개를 제출하고, 반 친구들이 교사가 만든 과제별로 작품을 둘러보며 좋아요와 피드백을 남길 수 있는 게시판을 구축한다.

- 프론트엔드: Streamlit
- 데이터베이스/백엔드: Supabase (PostgreSQL, RPC)
- 배포 대상: Streamlit Community Cloud 등 HTTPS 환경
- 기준 템플릿: `소스코드/index.html`, `app.js`, `style.css`

## 2. 템플릿에서 계승할 기능과 화면

| 구분 | 구현 내용 |
| --- | --- |
| 작품 갤러리 | 최신 등록순 카드 목록, 작품 링크 열기, 작성자·소개·과제 표시 |
| 과제 필터 | 전체 및 교사가 생성한 과제별 작품 필터링 |
| 교사 과제 관리 | 교사가 과제를 생성·수정·공개/마감 처리하고 학생 제출을 관리 |
| 작품 제출 | 과제, 닉네임, HTTPS 작품 주소, 짧은 소개 입력 |
| 좋아요 | 브라우저별 작품당 1회 좋아요, 누적 수 표시 |
| 피드백 | 작품별 피드백 목록 확인 및 닉네임·내용 등록 |
| 입력 보호 | 필수값, 길이, HTTPS URL, 금칙어 검사 및 사용자 안내 |
| 디자인 | 노랑·크림·청록 계열 카드형 반응형 레이아웃 |

Streamlit에서는 상단 탭(`st.tabs`)으로 **작품 갤러리 / 내 작품 제출**을 제공하고, 작품 카드는 컨테이너와 열(`st.columns`)을 조합해 구성한다. 템플릿의 CSS는 `st.markdown(..., unsafe_allow_html=True)` 및 `.streamlit/config.toml` 테마로 필요한 범위에서 재현한다.

## 3. 데이터 설계

### 테이블

`assignments` (교사가 생성하는 과제)

| 컬럼 | 형식 | 규칙 |
| --- | --- | --- |
| id | uuid | PK, 기본값 `gen_random_uuid()` |
| title | varchar(60) | 필수, 예: `STEP 1 · 인구·고령화 지도` |
| description | varchar(300) | 선택, 과제 안내 |
| due_at | timestamptz | 선택, 제출 마감 시각 |
| status | text | `draft`, `open`, `closed` 중 하나 |
| sort_order | integer | 갤러리/선택 목록 표시 순서 |
| created_at | timestamptz | 기본값 `now()` |
| updated_at | timestamptz | 수정 시 자동 갱신 |

`apps` (학생 작품)

| 컬럼 | 형식 | 규칙 |
| --- | --- | --- |
| id | uuid | PK, 기본값 `gen_random_uuid()` |
| assignment_id | uuid | `assignments.id` 참조, 필수 |
| nickname | varchar(20) | 필수 |
| url | text | 필수, `https://` 주소 |
| description | varchar(80) | 필수 |
| likes | integer | 기본값 0, 0 이상 |
| created_at | timestamptz | 기본값 `now()` |

`feedback` (작품 피드백)

| 컬럼 | 형식 | 규칙 |
| --- | --- | --- |
| id | uuid | PK, 기본값 `gen_random_uuid()` |
| app_id | uuid | `apps.id` 참조, 작품 삭제 시 함께 삭제 |
| nickname | varchar(16) | 필수 |
| content | varchar(60) | 필수 |
| created_at | timestamptz | 기본값 `now()` |

`profiles` (로그인 사용자 역할)

| 컬럼 | 형식 | 규칙 |
| --- | --- | --- |
| id | uuid | PK, 기본값 `gen_random_uuid()` |
| google_sub | text | Google OIDC의 고유 사용자 ID, unique |
| email | text | Google 계정 이메일, unique |
| display_name | varchar(20) | 화면에 표시할 이름 |
| role | text | `student` 또는 `teacher`, 기본값 `student` |
| created_at | timestamptz | 기본값 `now()` |

Google 로그인 성공 시 서버가 `profiles` 레코드를 확인하고, 없으면 `student` 역할로 생성한다. 교사 역할은 일반 앱 화면에서 바꿀 수 없고, 관리자가 Supabase Dashboard의 SQL Editor에서 해당 계정의 `profiles.role`만 `teacher`로 변경한다.

### RPC

`increment_likes(p_app_id uuid)`를 PostgreSQL 함수로 만든다. 함수 내부에서 `likes = likes + 1`을 수행하고 갱신 후 좋아요 수를 반환해 동시 클릭에서도 값이 안전하게 증가하도록 한다.

갤러리 조회는 `apps`와 `assignments`를 조인하여 과제 제목과 상태를 함께 가져온다. 과제를 삭제할 때 기존 작품을 잃지 않도록, 운영 화면에서는 기본적으로 **삭제 대신 마감/비공개 처리**를 제공한다.

## 4. 권한 및 안전 설계

1. Supabase secret/service key는 Streamlit 서버의 Secrets에만 저장하고 브라우저·저장소에 절대 노출하지 않는다.
2. Row Level Security(RLS)를 활성화한다.
   - Supabase 공개 API의 anon/authenticated 접근은 차단
   - 서버만 secret key로 데이터에 접근하고, Streamlit 로그인·역할 검사 후 필요한 작업만 수행
   - 좋아요는 사용자별 고유 기록과 DB 함수로 중복을 방지
3. Streamlit에서도 동일하게 필수값·문자 수·HTTPS URL·금칙어를 검사한다. DB 제약조건은 클라이언트 검증 우회에 대비한 최종 방어선이다.
4. 교사 권한은 Google OIDC 로그인 사용자와 `profiles.role = 'teacher'`를 기준으로 판별한다. 교사 전용 이메일 계정을 사전에 지정하거나, 관리자가 역할을 부여한다.
5. Google 로그인이 필수이므로 익명 사용자는 작품과 피드백을 쓸 수 없다.

초기 버전은 학생 닉네임 제출을 유지할 수 있으나, 교사 과제 관리에는 로그인 기반 권한 확인을 필수로 적용한다.

## 5. 로그인 방식과 사용자 흐름

**선택 방식: Streamlit Auth Provider의 Google OIDC 로그인**

1. 첫 화면에 `Google 계정으로 로그인` 버튼을 둔다. 학생과 교사는 학교 Google 계정으로 인증한다. 별도의 비밀번호 가입 폼은 제공하지 않는다.
2. 버튼은 Streamlit이 관리하는 Google OIDC 인증 흐름으로 이동한다. Google 인증이 끝나면 Streamlit 앱의 콜백 URL로 돌아온다.
3. Streamlit이 로그인 세션을 관리하고 앱은 Google의 사용자 ID·이메일로 `profiles` 역할을 조회한다.
4. 역할이 `student`이면 갤러리와 작품 제출만 표시한다.
5. 역할이 `teacher`이면 동일한 화면에 `과제 관리` 탭을 추가 표시한다. 이 탭에서만 과제 생성·수정·마감 기능을 제공한다.
6. 로그아웃을 누르면 Supabase 세션과 Streamlit 세션 정보를 모두 지운다.

Google로 처음 로그인한 사용자는 서버에 의해 자동으로 `student` 프로필을 받는다. 교사는 Google 로그인을 한 번 완료한 뒤, 관리자가 Supabase Dashboard의 SQL Editor에서 해당 이메일의 `profiles.role`만 `teacher`로 변경한다. 따라서 학생이 요청 값을 조작해도 교사 권한을 얻을 수 없다.

교사 탭을 숨기는 것 외에도, 서버 코드가 모든 과제 변경 요청 직전에 `profiles.role = 'teacher'`를 확인하도록 구현한다. Supabase 공개 API는 RLS와 권한 회수로 차단한다.

### Google OAuth 사전 설정

1. Google Cloud Console에서 OAuth 동의 화면을 만들고 웹 애플리케이션 OAuth Client ID를 생성한다.
2. Google Cloud Console의 승인된 JavaScript 원본에 배포 Streamlit 주소(예: `https://<app>.streamlit.app`)와 로컬 개발 주소를 등록한다.
3. Google Cloud Console의 승인된 리디렉션 URI에는 Streamlit 설정의 콜백 주소(예: `https://<app>.streamlit.app/oauth2callback`)를 정확히 등록한다.
4. `.streamlit/secrets.toml`의 `[auth.google]`에 Client ID/Secret과 Google OIDC metadata URL을 입력한다.
5. `.streamlit/secrets.toml`의 `[auth]`에 배포 앱의 로그인 콜백 URL(예: `https://<app>.streamlit.app/oauth2callback`)을 등록한다.
6. 실제 배포 URL이 확정된 뒤 설정한다. 개발·배포 환경은 각각 별도의 Google OAuth 클라이언트를 사용하는 것을 원칙으로 한다.

## 6. 구현 구조

```text
F:\info_board
├─ app.py                    # Streamlit 진입점·화면 렌더링
├─ requirements.txt          # streamlit, supabase 등 의존성
├─ .streamlit/
│  ├─ config.toml            # 테마
│  └─ secrets.toml.example   # 키 이름 예시(실제 키 제외)
├─ src/
│  ├─ database.py            # Supabase 클라이언트·조회·등록·좋아요 RPC
│  ├─ validation.py          # URL/길이/금칙어 검사
│  ├─ auth.py                # Google OAuth 시작·콜백·세션·교사 역할 확인
│  ├─ constants.py           # 상태값·문구·제한값
│  └─ styles.py              # 템플릿 기반 CSS
├─ sql/
│  └─ schema.sql             # 테이블, 인덱스, RLS, RPC
├─ tests/
│  └─ test_validation.py     # 순수 검증 로직 단위 테스트
└─ 소스코드/                 # 참고용 원본 템플릿(변경하지 않음)
```

환경 변수는 로컬 `.streamlit/secrets.toml` 또는 배포 서비스의 Secrets에 아래 이름으로 설정한다.

```toml
SUPABASE_URL = "https://<project-ref>.supabase.co"
SUPABASE_SERVICE_KEY = "<server-only-secret-key>"
```

## 7. 구현 순서

1. **기초 구성**: Python 가상환경 확인, `requirements.txt`, Streamlit 기본 앱과 테마 파일을 만든다.
2. **인증·권한 설계**: Streamlit Google OIDC Auth Provider와 `profiles` 역할 테이블을 구성하고 교사 계정을 지정한다.
3. **Supabase 스키마**: `schema.sql`에 과제·작품·피드백 테이블, 외래키, 인덱스, 제약조건, RLS, 좋아요 RPC를 작성하고 SQL Editor에서 적용한다.
4. **데이터 계층**: 과제 조회/관리, 작품·피드백 조회/등록, 좋아요 RPC 호출을 `src/database.py`에 분리한다.
5. **교사 화면**: 교사에게만 보이는 ‘과제 관리’ 탭에서 과제 생성, 안내·마감일 수정, 공개/마감 전환, 제출 현황 확인을 구현한다.
6. **검증 계층**: 길이·HTTPS·금칙어·과제 공개 상태·마감 시각 검사를 한 곳에 구현하고 사용자 친화적 오류 문구를 정한다.
7. **학생 화면**: 과제 필터, 최신순 카드, 외부 링크, 좋아요, 피드백과 제출 폼을 구현한다. 제출 폼의 과제 선택지는 현재 제출 가능한 과제만 표시한다.
8. **사용성 보완**: 빈 목록·네트워크 오류·로딩 상태, 모바일 1열 레이아웃, 새로고침 버튼을 추가한다.
9. **배포 준비**: Secrets 설정, Community Cloud 배포, 학생/교사 계정 각각으로 권한 동작을 최종 확인한다.

## 8. 검증 기준

- 등록된 작품이 새로고침 후에도 최신순으로 표시된다.
- 교사가 새 과제를 만들고 공개하면 학생 제출 목록과 갤러리 필터에 즉시 나타난다.
- 마감·비공개 과제는 학생의 새 제출 목록에 보이지 않으며, 기존 게시물의 보존 정책대로 표시된다.
- 일반 학생은 교사 과제 관리 화면과 관리 API를 사용할 수 없다.
- 과제 필터가 해당 과제 작품만 정확히 보여 준다.
- HTTP 주소, 빈 값, 제한 길이 초과, 금칙어 포함 입력은 저장되지 않는다.
- 두 사용자가 동시에 좋아요를 눌러도 누적 수가 정확하다.
- 피드백이 올바른 작품에 저장되고 새로고침 후에도 남아 있다.
- Google Client Secret과 Supabase service key가 저장소와 화면에 노출되지 않는다.
- 휴대폰 폭에서도 카드와 입력 폼을 사용할 수 있다.

## 9. 완료 산출물

- 실행 가능한 Streamlit 게시판 소스
- Supabase에 바로 적용 가능한 `sql/schema.sql`
- 환경 변수 예시와 실행·배포 방법을 담은 README
- 입력 검증 단위 테스트
